<?php

namespace {{NAMESPACE}};

use AiChat\Agents\BaseAgent;

class {{CLASS_NAME}} extends BaseAgent
{
    protected string $name = '{{AGENT_NAME}}';

    protected string $description = '{{AGENT_DESCRIPTION}}';

    protected string $systemPrompt = '{{SYSTEM_PROMPT}}';

    protected array $tools = [{{TOOLS}}];

    protected array $contextProviders = [{{CONTEXT_PROVIDERS}}];

    protected array $policies = [{{POLICIES}}];

    protected bool $readOnly = {{READ_ONLY}};
}
